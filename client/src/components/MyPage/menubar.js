import React from 'react';
import './menubar.css';

const MenuBar = () => {
  return (
    <div>
      <button>Collected</button>
      <button>Avtivity</button>
    </div>
  );
};

export default MenuBar;
